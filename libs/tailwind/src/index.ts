// CSS-only library — no TypeScript exports.
// Design tokens are consumed via CSS imports:
//   @import '@monorepo/tailwind/src/index.css';
//
// This file exists to satisfy the NX lint target
// (every library must have at least one TS file).
export {};
