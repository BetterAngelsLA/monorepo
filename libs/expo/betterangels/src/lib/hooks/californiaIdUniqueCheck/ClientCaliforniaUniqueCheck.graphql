query ClientCaliforniaUniqueCheck($filters: ClientProfileFilter) {
  clientProfilesPaginated(filters: $filters) {
    ... on ClientProfileTypeOffsetPaginated {
      pageInfo {
        offset
        limit
      }
      totalCount
      results {
        id
      }
    }
  }
}
