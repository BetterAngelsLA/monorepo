query hmisClientPrograms($clientId: ID!) {
  hmisClientPrograms(clientId: $clientId) {
    __typename
    ... on HmisClientProgramType {
      id
      program {
        id
        name
      }
    }
  }
}
