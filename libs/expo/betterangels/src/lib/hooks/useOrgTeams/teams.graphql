query Teams($filters: TeamFilter, $pagination: OffsetPaginationInput) {
  teams(filters: $filters, pagination: $pagination) {
    results {
      id
      name
      isActive
    }
    totalCount
  }
}
