mutation HMISLogin($email: String!, $password: String!) {
  hmisLogin(email: $email, password: $password) {
    ... on HmisLoginSuccess {
      user {
        id
      }
      refreshUrl
    }
    ... on HmisLoginError {
      field
      message
    }
  }
}
