query currentUser {
  currentUser {
    id
    username
    firstName
    lastName
    email
    organizations: organizationsOrganization {
      id
      name
      userPermissions
    }
    isOutreachAuthorized
    hasAcceptedTos
    hasAcceptedPrivacyPolicy
  }
}
