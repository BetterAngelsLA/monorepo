query currentUser {
  currentUser {
    id
    username
    firstName
    lastName
    email
    organizations: organizationsOrganization {
      id
      name
      permissions
    }
    isOutreachAuthorized
    hasAcceptedTos
    hasAcceptedPrivacyPolicy
    isHmisUser
  }
}
