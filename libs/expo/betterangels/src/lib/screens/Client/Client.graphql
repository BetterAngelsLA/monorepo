query ClientProfile($id: ID!) {
  clientProfile(pk: $id) {
    ... on ClientProfileType {
      id
      age
      dateOfBirth
      gender
      hmisId
      nickname
      preferredLanguage
      user {
        id
        email
        firstName
        lastName
        username
      }
    }
  }
}
