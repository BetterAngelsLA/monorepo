query ClientProfile($id: ID!) {
  clientProfile(pk: $id) {
    ... on ClientProfileType {
      id
      hmisId
      gender
      dateOfBirth
      preferredLanguage
      user {
        id
        email
        firstName
        lastName
        username
      }
    }
  }
}
