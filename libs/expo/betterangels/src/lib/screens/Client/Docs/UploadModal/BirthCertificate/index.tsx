import { TextRegular } from '@monorepo/expo/shared/ui-components';
import { ISectionProps } from '../types';

export default function BirthCertificate(props: ISectionProps) {
  const { setTab } = props;
  return <TextRegular>BirthCertificate</TextRegular>;
}
