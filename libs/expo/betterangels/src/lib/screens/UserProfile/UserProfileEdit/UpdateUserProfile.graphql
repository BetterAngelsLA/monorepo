mutation UpdateUserProfile($data: UpdateUserProfileInput!) {
  updateUserProfile(data: $data) {
    ... on CurrentUserType {
      id
      firstName
      lastName
    }
    ... on OperationInfo {
      messages {
        kind
        field
        message
      }
    }
  }
}
