mutation LoginWithGoogle($input: AuthInput!) {
  socialLogin(input: $input)
    @rest(
      type: "LoginPayload"
      path: "/api/login/social/session/"
      method: "POST"
    ) {
    username
    email
  }
}
