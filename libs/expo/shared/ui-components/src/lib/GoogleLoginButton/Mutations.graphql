mutation LoginWithGoogle($input: AuthInput!) {
  socialLogin(input: $input)
    @rest(
      type: "LoginPayload"
      path: "/login/social/session/"
      method: "POST"
    ) {
    username
    email
  }
}
