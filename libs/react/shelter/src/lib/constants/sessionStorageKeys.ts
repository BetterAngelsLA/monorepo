/** Key for the map center + zoom saved before navigating to a shelter detail. */
export const SESSION_STORAGE_MAP_CENTER = 'mapCenter';

/** Key for the full map bounds saved before navigating to a shelter detail. */
export const SESSION_STORAGE_MAP_BOUNDS = 'savedMapBounds';
