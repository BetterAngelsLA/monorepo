query currentOrgUser {
  currentUser {
    id
    username
    firstName
    lastName
    email
    organizations: organizationsOrganization {
      id
      name
      capabilities {
        accounts {
          canAccessPortal
          canManageMembers
          canViewMembers
        }
        reports {
          canViewReports
        }
      }
    }
  }
}
