query currentOrgUser {
  currentUser {
    id
    username
    firstName
    lastName
    email
    organizations: organizationsOrganization {
      id
      name
      permissions {
        accounts
        reports
        shelters
      }
    }
  }
}
