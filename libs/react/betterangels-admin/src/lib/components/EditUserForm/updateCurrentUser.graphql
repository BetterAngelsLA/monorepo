mutation UpdateCurrentUser($data: UpdateUserInput!) {
  updateCurrentUser(data: $data) {
    ... on OperationInfo {
      messages {
        kind
        field
        message
      }
    }
    ... on CurrentUserType {
      id
      firstName
      lastName
    }
  }
}
