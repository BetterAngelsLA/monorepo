query GetShelterOccupancyMetrics($shelterId: ID!) {
  shelterOccupancyMetrics(shelterId: $shelterId) {
    reservationMetrics {
      checkInOverdue
      cancelled
      checkedIn
      checkInOverdueToCheckedIn
    }
    avgDaysToOccupancy
  }
}
