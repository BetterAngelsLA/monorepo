/** Z-index for the invisible backdrop that captures outside clicks. */
export const Z_BACKDROP = 9998;

/** Z-index for the dropdown menu panel itself. */
export const Z_MENU = 9999;

/** Gap (in px) between the trigger's bottom edge and the menu's top edge. */
export const MENU_GAP = 6;
