mutation CloneRoom($id: ID!) {
  cloneRoom(id: $id) {
    ... on RoomType {
      id
      name
    }
    ... on OperationInfo {
      messages {
        kind
        field
        message
      }
    }
  }
}
