import type { OperationKey, ShelterTypename } from './types';

/** Verified against generated mutation types at compile time. */
export const OPERATION_KEY: OperationKey = 'updateShelter';

/** Verified against generated mutation types at compile time. */
export const PAYLOAD_TYPENAME: ShelterTypename = 'ShelterType';
