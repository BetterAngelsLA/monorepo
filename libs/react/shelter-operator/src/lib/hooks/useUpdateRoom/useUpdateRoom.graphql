mutation UpdateRoom($data: UpdateRoomInput!) {
  updateRoom(data: $data) {
    ... on RoomType {
      id
      name
      status
      type
    }
    ... on OperationInfo {
      messages {
        kind
        field
        message
      }
    }
  }
}
