mutation CreateBed($data: CreateBedInput!) {
  createBed(data: $data) {
    ... on BedType {
      id
      name
      status
      type
      room {
        id
        name
      }
    }
    ... on OperationInfo {
      messages {
        kind
        field
        message
      }
    }
  }
}
