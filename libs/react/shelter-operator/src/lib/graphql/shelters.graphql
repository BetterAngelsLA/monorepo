query ViewSheltersByOrganization(
  $organizationId: ID!
  $name: String
  $offset: Int
  $limit: Int
  $properties: ShelterPropertyInput
) {
  adminShelters(
    filters: { organizations: [$organizationId], name: $name, properties: $properties }
    ordering: [{ createdAt: DESC }]
    pagination: { offset: $offset, limit: $limit }
  ) {
    totalCount
    results {
      id
      name
      location {
        place
      }
      totalBeds
    }
  }
}
