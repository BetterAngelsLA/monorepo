query ViewSheltersByOrganization($organizationId: ID!) {
  adminShelters(
    filters: { organizations: [$organizationId] }
    ordering: [{ createdAt: DESC }]
  ) {
    results {
      id
      name
      location {
        place
      }
      totalBeds
    }
  }
}
