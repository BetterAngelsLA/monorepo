query ViewSheltersByOrganization(
  $organizationId: ID!
  $name: String
  $offset: Int
  $limit: Int
  $properties: ShelterPropertyInput
) {
  operatorShelters(
    filters: {
      organizations: [$organizationId]
      name: $name
      properties: $properties
    }
    ordering: [{ createdAt: DESC }]
    pagination: { offset: $offset, limit: $limit }
  ) {
    totalCount
    results {
      id
      name
      location {
        place
      }
      status
      totalBeds
      bedsByStatus {
        available
        occupied
        reserved
        outOfService
      }
      demographics {
        name
      }
      specialSituationRestrictions {
        name
      }
      shelterTypes {
        name
      }
      city {
        name
      }
      spa {
        id
        name
      }
    }
  }
}

query GetShelterName($id: ID!) {
  shelter(pk: $id) {
    id
    name
  }
}
