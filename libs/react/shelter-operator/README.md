# shelter-operator

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shelter-operator` to build the library.

## Running unit tests

Run `nx test shelter-operator` to execute the unit tests via [Jest](https://jestjs.io).
