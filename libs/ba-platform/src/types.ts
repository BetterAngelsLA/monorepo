/**
 * Type-only entry point — ``@monorepo/ba-platform/types``.
 *
 * Lightweight re-export of generated GraphQL types from the
 * BetterAngels backend schema. No runtime code, no framework
 * dependencies — safe for any consumer that only needs types.
 */
export * from './lib/apollo/graphql/__generated__/types';
