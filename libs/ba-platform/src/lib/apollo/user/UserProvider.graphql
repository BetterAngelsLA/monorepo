query currentOrgUser {
  currentUser {
    id
    username
    firstName
    lastName
    email
    permissions
    organizations: organizationsOrganization {
      id
      name
      permissions
    }
  }
}
