query currentOrgUser {
  currentUser {
    id
    username
    firstName
    lastName
    email
    organizations: organizationsOrganization {
      id
      name
      permissions {
        grants {
          domain
          values
        }
      }
    }
  }
}
