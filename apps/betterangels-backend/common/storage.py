# This module previously contained ImgproxyS3Storage.
# Imgproxy routing is now handled at the GraphQL layer (DjangoImageType.url()).
# The default storage backend is plain S3Storage configured in settings.py.
