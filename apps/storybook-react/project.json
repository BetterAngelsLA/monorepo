{
  "name": "storybook-react",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/storybook-react/src",
  "tags": [],
  "targets": {
    "storybook": {
      "executor": "@nx/storybook:storybook",
      "options": {
        "configDir": "apps/storybook-react/.storybook",
        "port": 4400
      }
    },
    "build": {
      "executor": "@nx/storybook:build",
      "outputs": ["{workspaceRoot}/dist/apps/storybook-react/storybook-static"],
      "options": {
        "configDir": "apps/storybook-react/.storybook",
        "outputDir": "dist/apps/storybook-react/storybook-static"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./tools/deploy/cloudfront_deploy.sh"
      },
      "configurations": {
        "preview": {}
      },
      "dependsOn": ["build"]
    },
    "post-pr-preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/post-preview-comment.js --url=https://admin.dev.betterangels.la$VITE_APP_BASE_PATH"
      }
    }
  }
}
