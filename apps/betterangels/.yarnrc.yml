# Minimal Yarn config for EAS Build — enables traditional node_modules/
# so Expo config plugins can be resolved at runtime.
# The workspace root .yarnrc.yml is NOT copied because it references
# a custom yarnPath that doesn't exist on EAS servers.
nodeLinker: node-modules
