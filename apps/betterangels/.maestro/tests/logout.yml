appId: ${MAESTRO_APP_ID}
---
- runFlow: ../lib/framework/start_test.yml

- assertVisible:
    id: authorized-root

# temporary replacement for navigate_via_menu flow
- runFlow:
    file: ../lib/navigation/open_app_menu.yml

- assertVisible: 'Log Out'
# disable logout test temporarily
# this ticket hopefully fixes it - DEV-2497: improve flakiness in CI
# follow-up ticket - DEV-2511: reinstate logout test
# - runFlow:
#     file: ../lib/navigation/navigate_via_menu.yml
#     env:
#       MENU_ITEM: 'Log Out'

# - assertVisible:
#     id: unauthorized-root
