# Add files here to ignore them from prettier formatting
/dist
/coverage

/.nx/cache
/.nx/workspace-data
.nx/self-healing

# GraphQL codegen output — regenerated by ynx-generate, don't format
**/__generated__/**

# Django templates use {% %} syntax — handled by djlint, not Prettier
**/templates/**/*.html
